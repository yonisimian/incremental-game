dist
node_modules
pnpm-lock.yaml

# Canonical tree files — the single source of truth, edited via the dev-page
# tree editor. Their format is owned by `serializeTree` (the editor's export),
# so Prettier must not reformat them.
shared/trees
