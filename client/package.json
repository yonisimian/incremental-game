{
  "name": "client",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "~4.1.5",
    "typescript": "~6.0.3",
    "vite": "~8.0.10",
    "vitest": "~4.1.5"
  },
  "dependencies": {
    "@game/shared": "workspace:*"
  }
}
